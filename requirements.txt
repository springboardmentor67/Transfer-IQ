pandas>=2.2
numpy>=2.0
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
python-dotenv>=1.0
tqdm>=4.66
matplotlib>=3.9
seaborn>=0.13
vaderSentiment>=3.3.2
textblob>=0.18.0
tabulate>=0.9
# Twitter API client (optional; required for week1_twitter_sentiment.py)
tweepy>=4.14
scikit-learn>=1.5
xgboost>=2.1
# TensorFlow wheels are not yet available for some newer Python versions on Windows (e.g., 3.14).
# The core project (Streamlit UI, data pipeline, Torch LSTM) does not require TensorFlow at runtime.
tensorflow>=2.17; python_version < "3.13"
jupyter>=1.0
emoji>=2.12
torch>=2.0.0
streamlit>=1.35.0

# API runtime (app.py)
fastapi>=0.110
uvicorn[standard]>=0.23
joblib>=1.3

plotly
shap

